# ---- Empaquetado / build ----
build/
dist/
*.egg-info/
.eggs/

# ---- Entornos virtuales (el venv de pipx vive fuera; estos son los antiguos
#      en el repo y los de diarizadores, que ahora van en la carpeta de datos) ----
venv/
venv-dia-community1/
venv-dia-nemo/

# ---- Binarios grandes (los baja `transcribe --start` desde gyan.dev) ----
ffmpeg.exe
ffprobe.exe

# ---- Cache de Python ----
__pycache__/
*.py[cod]

# ---- Artefactos de ejecucion (ahora viven en la carpeta de datos del usuario,
#      pero por si quedan sueltos en el repo) ----
benchmarks.jsonl
last_status.json
.transcribe_status.json
_diar_in.wav
_diar_out.json
_diar_out.stats.json

# ---- Material de prueba (audios pesados + sus salidas) ----
pruebas/

# ---- Salidas de transcripcion sueltas en la raiz ----
*.srt
*.vtt
*.tsv

# ---- Sistema operativo / editores ----
.DS_Store
Thumbs.db
.vscode/
.idea/
